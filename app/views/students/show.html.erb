Student: <%= @student.name %>

<% @student.courses.each do |course| %>
  <section id='course-<%=course.id%>'>
    <p>Course: <%= course.name %></p>
    <p>Grade: <%= @student.grade(course) %></p>
  </section>
<% end %>
